import styled from 'styled-components'

export const PlaygroundContent = styled.article`
  background: var(--dark-alt);
  padding: var(--scale-5) var(--scale-5) var(--scale-3);
  margin-top: var(--scale-5);
  :last-of-type {
    margin-bottom: var(--scale-8);
  }
`

export const PlaygroundHeading = styled.h3`
  margin: 0;
`

export const PlaygroundInfo = styled.div`
  margin-top: var(--scale-3);
`
